name: KawaiiRawEditor

options:
  deploymentTarget:
    iOS: "16.0"
  bundleIdPrefix: com.dueckis

targets:
  KawaiiRawEditor:
    type: application
    platform: iOS
    sources:
      # This tells Xcode to look for Swift files in a folder named 'Sources'
      - Sources
    info:
      path: Info.plist
      properties:
        CFBundleDisplayName: KawaiiRawEditor
        CFBundleShortVersionString: "0.0.1"
        CFBundleVersion: "1"
        # Fixes black screen crash on launch
        UILaunchScreen: {}
        # Required for picking files
        NSPhotoLibraryUsageDescription: "This app needs gallery access to edit photos."
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.dueckis.kawaiiraweditor
        SWIFT_VERSION: "5.0"
        TARGETED_DEVICE_FAMILY: "1,2" # iPhone & iPad

        # Point Xcode to where Gradle builds the Kotlin XCFramework
        FRAMEWORK_SEARCH_PATHS: "$(inherited) $(SRCROOT)/../shared/build/XCFrameworks/debug"
        LD_RUNPATH_SEARCH_PATHS: "$(inherited) @executable_path/Frameworks"

        # Code signing is required for running on a real device.
        # Configure your team/provisioning profile in Xcode after generating the project.

    dependencies:
      - framework: ../shared/build/XCFrameworks/debug/KawaiiShared.xcframework
        embed: true
        codeSign: false

schemes:
  KawaiiRawEditor:
    build:
      targets:
        KawaiiRawEditor: all
